# ── Монорепо Luminary: корневые игноры ──
# Детальные правила для десктопа — в apps/desktop/.gitignore

# Сессионные файлы ZCode (не часть проекта)
.zcode/

# Локальные артефакты
.DS_Store
Thumbs.db

# Сборки Android (Capacitor) — появятся с этапом apps/android
apps/android/android/.gradle/
apps/android/android/local.properties
apps/android/node_modules/
*.apk
*.aab
